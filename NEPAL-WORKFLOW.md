# Nepal ICT briefing workflow

The user changed the active market to Nepal on 2026-09-18. This explicit instruction supersedes prior three-country workflows. Historical reports and market-backup inputs are archival only.

- Read `config/market.json`, `config/sources.json`, and `config/topic-source-routing.json` before research. All active content must belong to Nepal. Preserve the current GitHub Pages URL.
- Update this checkout from main before work. Do not run `scripts/initialize-nepal.mjs` again: it is a one-time migration, not a recurring refresh.
- Daily public-link collection: `python3 scripts/collect_nepal_sources.py`. `config/nepal-source-candidates.json` contains unreviewed links with observation dates, never assumed publication dates. A readable homepage does not establish that every post was scanned. Log inaccessible sites and authentication barriers.
- Research Nepali and English, verify Bikram Sambat dates before conversion, and publish Chinese and English. Use official reverse links for account ownership. Verify date, full article content, and business relevance; independently corroborate secondary media. Equipment-vendor global news is included only when a Nepal customer, project, deployment or local market is explicit.
- Cover NTA, MoCIT, PMO, Nepal Telecom, Ncell, WorldLink, Vianet, Subisu, other ISPs, equipment vendors, ICT/device media, banking, public procurement, investment and tax. Maintain eleven report sections with honest empty states.
- Telegram remains one shared input to all topics. Only verified Nepal public channels may be enabled in `config/sources.json` using `identityVerified: true` and a `verificationUrl`. Existing GitHub Secrets can be reused; never copy credentials into source code, logs or news. No private dialogs or automatic joins. No verified channel is a visible coverage gap, not justification to fall back to the former market.
- `scripts/audit_subscribed_telegram.py` identifies subscribed public Nepal candidates. Discovery is not ownership verification. Never publish names or messages from private chats.
- Curate `config/nepal-report.json` with complete bilingual fields per story; derive counts from actual items. Keep the same issue within a biweekly period. Do not overwrite former-market archive HTML.
- Curate `config/social-signals.json` plus `config/social-signal-translations-en.json`, and both Telegram translation dictionaries. Preserve stable IDs and deduplicate events. After research, update status fields in the report to reflect real coverage.
- Maintain `config/nepal-people.json` from official office-holder lists, with links and verification dates. Do not carry forward former-market names or infer an incumbent from old reporting.
- Maintain `config/nepal-legal-news.json` from NTA, MoCIT, Nepal Gazette, Nepal Law Commission, NRB, PPMO, IRD and Investment Board. Distinguish proposal, publication, commencement and enforcement. Legal statements require official documents. Keep the legal-opinion disclaimer.
- `config/compliance-analysis.json` retains six weights 20/20/15/20/15/10. Assign Nepal scores only with current Nepal evidence. Until then display pending, never inherited scores.
- Generate data using `npm run sync-data`; never edit data outputs manually. Run `npm run build`, `npm audit --omit=dev`, and `git diff --check`. Reject non-Nepal active inputs, missing translations, invalid official legal URLs and unsupported claims.
- Commit only relevant changes, push main under the user's standing deployment authorization, wait for Pages success, and check overview, briefing, compliance, officials, sources and archives in Chinese and English. When source gaps prevent a complete news refresh, report the specific gaps; a configuration migration may ship with a clearly labeled pending-news state.
- Weekly editorial refresh remains Sunday 02:00 Asia/Baghdad (07:45 Nepal time) unless the user changes the time. Daily web/Telegram precollection is 01:30 Nepal time. Scheduling delays must not be reported as on-time completion.
