import { Radio, ExternalLink, CheckCircle2 } from "lucide-react";
import { Badge, Card } from "@/components/ui";
import sources from "@/data/sources.json";

export default function SourcesPage(){const normal=sources.filter(s=>s.status==="正常").length;const posts=sources.reduce((sum,s)=>sum+s.posts14d,0);const t1=sources.filter(s=>s.tier==="T1").length;return <><div className="hero"><div><div className="eyebrow">Source Operations</div><h1>Telegram 信源中心</h1><p>官方频道健康状态、责任人物和最近14天采集量</p></div><Badge tone="green"><CheckCircle2 size={11}/> {normal}/{sources.length} 正常</Badge></div><div className="grid kpi-grid"><Card className="card-pad"><div className="kpi-label">接入频道</div><div className="kpi-value">{sources.length}</div><div className="kpi-note">均为政府机构频道</div></Card><Card className="card-pad"><div className="kpi-label">14天消息</div><div className="kpi-value">{posts}</div><div className="kpi-note">自动采集成功</div></Card><Card className="card-pad"><div className="kpi-label">T1核心源</div><div className="kpi-value">{t1}</div><div className="kpi-note">通信部、CMC、外交部</div></Card><Card className="card-pad"><div className="kpi-label">失败源</div><div className="kpi-value">{sources.length-normal}</div><div className="kpi-note">会话状态正常</div></Card></div><Card className="table-card"><div className="table-wrap"><table className="data-table"><thead><tr><th>状态</th><th>机构</th><th>关联人物</th><th>频道</th><th>层级</th><th>14天消息</th></tr></thead><tbody>{sources.map(s=><tr key={s.handle}><td><Badge tone="green"><CheckCircle2 size={10}/> {s.status}</Badge></td><td><strong>{s.name}</strong></td><td>{s.owner}</td><td><a className="social-link" href={`https://t.me/${s.handle}`} target="_blank" rel="noreferrer"><Radio size={10}/>@{s.handle}<ExternalLink size={9}/></a></td><td><Badge tone={s.tier==="T1"?"red":"default"}>{s.tier}</Badge></td><td>{s.posts14d}</td></tr>)}</tbody></table></div></Card></>}
